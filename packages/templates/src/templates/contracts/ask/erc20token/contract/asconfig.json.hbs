{
    "extends": "ask-lang/asconfig.json",
    "targets": {
        "debug": {
            "binaryFile": "build/{{contract_name}}.debug.wasm",
            "textFile": "build/{{contract_name}}.debug.wat"
        },
        "release": {
            "binaryFile": "build/{{contract_name}}.wasm",
            "textFile": "build/{{contract_name}}.wat"
        }
    }
}
